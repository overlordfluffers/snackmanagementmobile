import {Text, View, SafeAreaView} from "react-native";
import React from "react";

const AccountPage = (props) => {
    return React.useMemo(()=>(
        <View>
            <Text>This is a account page</Text>
        </View>
    ), [])
}

export default AccountPage